#include<bits/stdc++.h>
using namespace std;
using LL = long long;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int T = 1000;
    while(T--) {
        for(int i = 1; i <= 5; ++i) {
            string st;
            cin << st;
            if (st[0] <= '9') 
        }
    }
    return 0;
}